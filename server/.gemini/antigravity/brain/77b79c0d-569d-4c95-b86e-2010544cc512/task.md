# Task Plan

- [x] Investigate how "Alumni" are represented in the backend database (e.g. `Student` model with specific status or year).
- [x] Create a backend API endpoint to fetch alumni profiles (Found existing `/api/student-features/alumni`).
- [x] Investigate the structure of the existing `AlumniNetwork` frontend component.
- [ ] Add the `fetchAlumni` function to `client/src/services/api.js`.
- [ ] Integrate the API into the `AlumniNetwork` component to display real data instead of dummy data.
